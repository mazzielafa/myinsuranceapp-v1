import json
import unittest
import requests

class TestApp(unittest.TestCase):
    token=''
    base_url='http://localhost:5000/api/v1'

# In all methods we need to use the token
# Need this to be running

# GET METHOD
# POST METHOD
# GET - If the user is given an incorrect token

